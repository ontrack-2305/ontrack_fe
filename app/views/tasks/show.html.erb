

  <img src=<%= @task.image %>>

 
<%= render partial: 'form', locals: {
  url: task_path(@task.id),
  method: :patch, 
  button_text: "Save Changes"
  }
  %><br>
<% # can't test whether confirm to delete shows up- test this when BE works and can spin up a server %>

 
